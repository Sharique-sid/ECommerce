spring.application.name=ecommerce-platform-dev

# Server Configuration
server.port=8081
server.servlet.context-path=/

# Use MySQL for persistent data storage
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=Sharique_sid
spring.datasource.password=Sharique@16
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.root=INFO
logging.level.com.ecommerce=DEBUG

# JWT (dev) - Must be at least 512 bits (64 characters) for HS512
app.jwtSecret=myVeryLongAndSecureJwtSecretKeyForDevelopmentPurposesOnlyNeverUseThisInProductionEnvironment2024!
app.jwtExpirationMs=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:5173
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.max-age=3600

# Email Configuration (Replace with your actual credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=clashgod1607@gmail.com
spring.mail.password=myxrpnedqqwnujhv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
